#include <stdio.h>
/**
 * main - Entry function
 *
 * Return: Always 0 (Success/correct)
 */

int main(void)
{
	int number;

	for (number = 48 ; number < 58 ; number++)
	{
		putchar (number);
	}

	putchar ('\n');

	return (0);
}
