#include <stdio.h>
/**
 * main- Entry point
 * puts function
 * Return : return 0 after execution
 */
int main(void)
{
	char *str = "Programming is like building a multilingual puzzle";
	puts(str);
	return (0);
}
